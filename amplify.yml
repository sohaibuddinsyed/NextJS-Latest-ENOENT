version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - pnpm install
        - ./patch-next.sh
    build:
      commands:
        - pnpm run build
        - ln -s . node_modules/symLink
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
